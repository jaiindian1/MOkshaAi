{
  "name": "moksha-ai-proxy",
  "version": "1.0.0",
  "description": "Secure proxy server for the Moksha AI Gemini implementation.",
  "main": "server.js",
  "type": "module", 
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/genai": "^latest",
    "cors": "^latest",
    "dotenv": "^latest",
    "express": "^latest"
  }
}
